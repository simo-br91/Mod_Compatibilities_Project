# ML

Deterministic Phase 5 ML-adjacent infrastructure lives here: offline feature extraction, replayable risk calibration, and grounded pack-review summary generation. The current slice is intentionally inspectable and fixture-backed rather than opaque model inference.
