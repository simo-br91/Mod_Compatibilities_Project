[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "modcompat-ml"
version = "0.1.0"
description = "Phase 5 deterministic ML service for feature extraction, calibrated risk scoring, and grounded summaries"
requires-python = ">=3.12"
