# Workspace

Identity, organizations, memberships, workspaces, projects, API keys, and usage boundaries live here. Billing is intentionally deferred but the ownership boundary matches the source documents.

